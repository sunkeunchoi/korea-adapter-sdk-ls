tr_code: S3_
name: KOSPI 체결 실시간 시세
owner_class: realtime
facets:
  protocol: websocket
  instrument_domain: stock
  venue_session: krx_regular
  date_sensitive: false
  self_paginated: false
  account_state: false
  paper_incompatible: false
  certification_path: automated
  rate_bucket: market_data
  caller_supplied_identifiers: [shcode]
dependencies:
  self_continuation_fields: []
  strong_order_fields: []
support:
  tracked: true
  implemented: true
  recommended: true
maintenance:
  source_spec_hash: e5f6a1b2c3d4
  last_reviewed: 2026-06-17
recommendation:
  behavior: Paper websocket subscribe lifecycle reachability (connect → subscribe → unsubscribe) on the paper port
  evidence_ref: evidence/S3_.yaml
  excludes:
    - Trade-data correctness (the smoke proves lifecycle, not row contents)
    - In-session row delivery guarantees (a row is bonus, not required)
    - Reconnection semantics after a dropped connection
    - Production-credential feeds (evidence is paper only)
    - Stronger freshness automation than the stated policy (no enforcement in code today)
