tr_code: CFOAT00100
name: 선물옵션 정상주문 (domestic F/O order submit)
owner_class: orders
facets:
  protocol: rest
  instrument_domain: futures_options
  venue_session: krx_regular
  date_sensitive: false
  self_paginated: false
  account_state: true
  paper_incompatible: false
  certification_path: manual
  rate_bucket: orders
  caller_supplied_identifiers: [FnoIsuNo]
dependencies:
  self_continuation_fields: []
  strong_order_fields: [FnoIsuNo, BnsTpCode, OrdQty, FnoOrdPrc]
support:
  # Implemented: the domestic F/O order SUBMIT leg certified on a clean in-window
  # guarded F/O paper order-chain smoke (rsp_cd 00040 모의투자 매수주문 완료;
  # `make live-smoke-fo-order`, plan 2026-07-01-001 U3, KRX open 2026-07-01). The
  # submit->modify->cancel chain rested a daily-limit buy (cannot fill), then t0441
  # positively confirmed no fill after a clean cancel — the account was left flat.
  # Recommended deferred (live order-placement endorsement, separate pass).
  tracked: true
  implemented: true
  recommended: false
maintenance:
  source_spec_hash: a1b2c3d4e5f6
  last_reviewed: 2026-06-30
