tr_code: CSPBQ00200
name: 현물계좌증거금률별주문가능수량조회
owner_class: account
facets:
  protocol: rest
  instrument_domain: stock
  venue_session: unspecified
  date_sensitive: false
  self_paginated: false
  account_state: true
  paper_incompatible: false
  certification_path: automated
  rate_bucket: account
  caller_supplied_identifiers: [IsuNo]
dependencies:
  self_continuation_fields: []
  strong_order_fields: []
support:
  # PENDING (not flipped) — the 2026-06-30 in-window paper smoke
  # (`make live-smoke-cspbq00200`) returned a structurally-valid row
  # (rsp_cd=00136, 1 row) but every substantive capacity field defaulted to zero
  # (Dps / PrsmptDpsD1 / SeOrdAbleAmt all non_default=false): the paper account
  # carries no cash deposit, so margin-rate orderable quantity is funding-gated to
  # zero. No orderable-quantity witness to assert, so the read stays Tracked, not
  # Implemented (R2/R3/AE1). Verified not a wrong-credential-lane artifact — this is
  # a 현물(spot)-account read on the default paper credential lane (the correct lane).
  tracked: true
  implemented: false
  recommended: false
maintenance:
  source_spec_hash: c1d2e3f4a5b6
  last_reviewed: 2026-06-30
