# Focused Evidence — t1101 (current-price + order-book quote)
#
# A durable, credential-free record of a genuine Paper Live Smoke that fetched a
# live order-book snapshot. The `line` below is captured VERBATIM from the
# `LIVE-SMOKE` stdout of `make live-smoke-book`
# (crates/ls-sdk/tests/live_smoke.rs::live_smoke_book), which is credential-free
# and self-dated by construction (no rsp_msg, machine-emitted UTC date). This
# file is the link between t1101's `maintenance.last_reviewed` date and the run
# that justifies it; the filename (keyed to the TR code) plus the matching date
# is the link.
#
# Secret-safety: structural descriptors and public market data only — never the
# token, appkey, secret, or account number. `symbol=005930` is a public KOSPI
# ticker; `price`/`offerho1`/`bidho1` are public market quotes.
#
# Integrity of THIS record: `rsp_cd=00000` (gateway success) plus a non-empty
# order book (`offerho1`/`bidho1`) confirm a genuine market-data round-trip — not
# a paper-guard miss (which fails the test) and not a 01900 paper-incompatible
# refusal (which would Err before recording).
tr_code: t1101
date: 2026-06-17
env: paper
target: live-smoke-book
line: "LIVE-SMOKE target=live-smoke-book inputs=[env=paper symbol=005930 date=2026-06-17] result=[token_len=380 rsp_cd=00000 price=346500 offerho1=346500 bidho1=346000]"
