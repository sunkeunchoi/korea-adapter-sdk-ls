{
  "_note": "FROZEN pre-registration mirror (KTD9) for the ORB Production Ladder. RE-REGISTERED to v2 for head v34: the strategy code hash changed v30 (6ae7b9f1…) → v34 (d7a9820b…), a code_change_resets_to_rung_1 event, so the economic expectation bands are re-derived from the head v34 backtest closed-trade distribution (20260724T014752Z-backtest-orb-v34, 24 KST sessions) via the identical Protective formula — see config/PREREGISTRATION.md § Re-registration v2 (head v34) for the per-value derivation and its backtest source. This is a legitimate re-registration (backtest-derived, head genuinely changed), NOT fitting to live data. Amendable ONLY by an explicit re-registration dispatch. Every dispatch record cites the SHA-256 of this file. Rung tracking_bands are SCHEDULED: each rung's band freezes from the PRECEDING rung's LIVE data before the first session at that rung (KD3/KD6) — they are intentionally absent here (the backtest has zero slippage, so they cannot be grounded now). Rung 1 carries no tracking_band by design; a rung-2+ dispatch is fail-closed until its band is re-registered. session_max_loss_krw stands at 300,000 (KTD3): ~2.2× v34's worst rung-1 session (−1,360,330 × 0.10 = −136,033); re-register a larger breaker before rungs 3–4.",
  "version": 2,
  "k_window": 5,
  "heartbeat_interval_secs": 90,
  "session_max_loss_krw": 300000.0,
  "rung0_requalification": "Re-entry to rung 1 requires ALL of: (1) the triggering limit event's root cause identified and written into the re-registration reason (elapsed time alone never qualifies — a benign-cause watchdog trip, KD5, still requires this); (2) at least 3 clean paper sessions run through the dispatch gate at LS_TRADING_ENV=paper since the suspension, each finalized with zero limit events; (3) attended operator re-registration with a fresh nonce.",
  "exceedance": {
    "max_reconcile_advised": 1,
    "max_deferrals": 3,
    "max_coverage_gaps": 1
  },
  "head_change": {
    "params_change_reruns_n": true,
    "code_change_resets_to_rung_1": true
  },
  "rungs": [
    { "rung": 1, "fraction": 0.10, "n_clean_sessions": 5,
      "expectation_band": { "min_cum_pnl": -148000.0, "max_cum_pnl": 266000.0 } },
    { "rung": 2, "fraction": 0.25, "n_clean_sessions": 5,
      "expectation_band": { "min_cum_pnl": -371000.0, "max_cum_pnl": 665000.0 } },
    { "rung": 3, "fraction": 0.50, "n_clean_sessions": 5,
      "expectation_band": { "min_cum_pnl": -742000.0, "max_cum_pnl": 1330000.0 } },
    { "rung": 4, "fraction": 1.00, "n_clean_sessions": 5,
      "expectation_band": { "min_cum_pnl": -1483000.0, "max_cum_pnl": 2659000.0 } }
  ]
}
