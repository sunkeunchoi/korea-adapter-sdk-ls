{
  "refill_secs": 120,
  "window_secs": 86400,
  "budget_calls": null,
  "bucket_scope": "per_credential",
  "provenance": "MEASURED via budget-probe 2026-07-09 (paper, .env.domestic_option, probe retargeted from t1102 to t8412 = the minute-ingest TR). FINDING: the cold IGW00201 budget is LARGE and was never exhausted within the probe ceiling — t8412 served >=600 calls @1/s (10 min sustained), t1102 >=1200 @10/s. It IS a cumulative rolling budget, NOT pure rate: turn-3 recorded PACED requests tripping after ~13 t8412 pages once the budget was WARM (heavy same-day multi-source spend). The probe ran from a relatively cold t8412 bucket, so it only established a large lower bound, not the ceiling. budget_calls stays null on purpose: the true ceiling is unmeasured, so pinning any number would be a wrong (too-low) cap that provokes needless deferrals; keeping plan-ahead OFF + the U3 IGW00201 recovery arm as backstop + the per-symbol drip loop for warm-budget ingests is the safe, evidence-backed choice. refill_secs=120 is the turn-3 field-proven recovery backoff, NOT probe-measured (stage 2 recorded no refill because stage 1 never exhausted). bucket_scope=per_credential from stage-0 plus a heavily-t1102-spent key still serving >=600 t8412 calls (no cross-TR depletion observed at these volumes). window_secs left at the day default: unmeasured. To measure the ceiling+refill, warm the t8412 budget first, then probe with a much higher LS_PROBE_CEILING on that warm bucket."
}
