//! `lab-research` — the loop-turn CLI (U1–U6): turn / runs compare / replay /
//! catalog status / analyze --scaffold over the append-only run registry.
fn main() -> std::process::ExitCode {
    nautilus_ls_lab::runner::research::main_cli()
}
