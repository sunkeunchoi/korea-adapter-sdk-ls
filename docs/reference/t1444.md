# SDK Reference: t1444

시가총액상위

> Generated from `ls-metadata` — do not edit by hand. Run `make docs` to regenerate.

- Owner class: `paginated`

## Recommendation

**Recommended behavior:** Paper single-page per-board market-cap ranking read (시가총액상위, upcode="001" KOSPI, idx=0 first page) through the self-paginated body-cursor response shape — a genuine round-trip (rsp_cd=00000, rows=20)

- Evidence: `evidence/t1444.yaml` (environment: `paper`)
- Freshness date: `2026-08-14` (`maintenance.last_reviewed`)
- Review by: `2026-11-12` (freshness date + 90-day backstop)
- What would revoke this claim: the **90-day backstop is enforced** — `make freshness-check` flags this TR's Focused Evidence as stale once 90 days elapse from the freshness date (the review-by date above), and the recommendation must then be re-attested. **Change-driven staling is also enforced** — a qualifying Structural API Shape change (field add/remove/change or endpoint/protocol change) diverging from the attested shape stales the evidence (advisory, surfaced by the same check); only *auto-revoke* of the recommendation is deferred (a human re-attests or demotes). Description / `korean_name` / rate-limit / reorder changes are non-qualifying and do not stale it. See `metadata/EVIDENCE-FRESHNESS.md`.

This recommendation does not claim:

- Production-credential reads (evidence is paper only)
- Per-board correctness beyond KOSPI (the smoke exercised upcode="001" only; the universe capture also walks 301 KOSDAQ, which this evidence does not certify)
- Multi-page ranking correctness (the smoke proves the first page on the idx cursor; continuation beyond one page is uncertified)
- Completeness or stability of the ranking (the smoke proves a non-empty ordered page, not that the board's membership or its ordering is exhaustive or reproducible across calls)
- Row-value correctness beyond the round-trip (price/change/diff/volume/total/rate/for_rate are point-in-time quotes; the smoke asserts a structural count, never values)
- Correctness of the market-cap ordering itself (the `total` column and the rank it implies are taken as served; nothing here independently verifies the cap arithmetic or its as-of instant)
- In-session serving during an open KRX window (both the smoke and the differential probe ran OFF-session on 2026-08-14 — past 15:30 KST, and the day itself reads Unknown on the KRX calendar snapshot, so this evidence cannot claim a proven trading session either way; the TR is certified under closure, so in-window serving is uncertified by THIS evidence rather than assumed)
- Intra-day freshness of the price-bearing ranking columns during halts/VI or across session transitions (the quote columns are not certified fresh)
- Gateway-side rejection of an omitted upcode — preflight enforces it locally, the gateway tolerates its absence and defaults the board (gateway_tolerant; the differential probe reads the accepted violation as expected-tolerant). This exclusion is load-bearing for consumers that read one board at a time — a defaulted board returns the wrong market's ranking under a successful rsp_cd
- Point-in-time historical ranking (the board is a current-state read; using it to tier a PAST window is a disclosed look-ahead the consumer owns, not something this recommendation licenses)
- Stronger freshness automation than the stated policy (no enforcement in code today)

## Errors & validation

Preflight validation runs before any network call; an invalid request is rejected locally (`LsError::Invalid`) with no HTTP call. Type and required-ness always enforce; a value-class bound (enum/range/format) is permissive until the differential probe confirms it, so a valid request is never falsely rejected.

**Request field rules:**

- `upcode` — type `string`; required; gateway-tolerant on [required] (preflight enforces; gateway does not)
- `idx` — type `integer`; required

**Reachable gateway errors** (explained once from the shared catalog; environment/entitlement codes are not reproduced per TR):

- `00000` — Success. The gateway processed the request and returned valid data.
- `00136` — Inquiry completed (조회가 완료되었습니다). A read succeeded and the response carries data. Treated as success by the read path.
- `00707` — Inquiry completed with an empty result set (조회할 내역이 없습니다). The query ran successfully but matched no rows. Treated as success, not an error.
- `904` — The market session required for this TR is closed. An environment condition, not a request defect: the same request succeeds inside the correct trading window. Retry in-session.
- `01900` — This service is not provided in the Paper (모의투자) environment (모의투자에서는 해당업무가 제공되지 않습니다). A per-service capability gate: no Paper account can clear it. Not a problem with your request. Retry against a Production credential set, or treat the TR as paper-incompatible.
- `IGW40011` — A request field the gateway expects as a JSON number was sent as a quoted string. Serialize numeric request fields as JSON numbers (`string_as_number` / `string_as_decimal`) rather than strings. This is a request-shape defect the caller can fix.

