# SDK Reference: CSPAT00601

현물 정규주문 (cash equity order submission)

> Generated from `ls-metadata` — do not edit by hand. Run `make docs` to regenerate.

- Owner class: `orders`

## Recommendation

**Recommended behavior:** Paper cash-equity order submit of a single caller-owned resting buy (the opening leg of the submit→modify→cancel chain; rsp_cd 00040, account confirmed flat after)

- Evidence: `evidence/CSPAT00601.yaml` (environment: `paper`)
- Freshness date: `2026-07-22` (`maintenance.last_reviewed`)
- Review by: `2026-10-20` (freshness date + 90-day backstop)
- What would revoke this claim: the **90-day backstop is enforced** — `make freshness-check` flags this TR's Focused Evidence as stale once 90 days elapse from the freshness date (the review-by date above), and the recommendation must then be re-attested. **Change-driven staling is also enforced** — a qualifying Structural API Shape change (field add/remove/change or endpoint/protocol change) diverging from the attested shape stales the evidence (advisory, surfaced by the same check); only *auto-revoke* of the recommendation is deferred (a human re-attests or demotes). Description / `korean_name` / rate-limit / reorder changes are non-qualifying and do not stale it. See `metadata/EVIDENCE-FRESHNESS.md`.

This recommendation does not claim:

- Order submissions for symbols or accounts the caller does not own beyond the single-symbol paper submit exercised
- Production-credential order submissions (evidence is paper only)
- Fill / settlement or resulting-position correctness beyond a successful submit-accepted (rsp_cd 00040) round-trip
- Gateway-side rejection of an omitted booking-determining field — BnsTpCode (direction) plus the provisional OrdprcPtnCode/OrdCndiTpCode/MgntrnCode: these required variants are NEVER fired by the differential (booking_determining / Route C) because omission places a real direction-defaulted order (§30 ordno=17093). The claim rests on the preflight-enforced caller contract, not gateway-side rejection of the omission.
- Rejection semantics of the three provisional (R11) booking-determining fields, which have no live characterization — they are annotated fail-closed and characterizable only via the attended booking A/B harness (make live-smoke-cspat00601-booking-ab)
- Stronger freshness automation than the stated policy (no enforcement in code today)

## Errors & validation

Preflight validation runs before any network call; an invalid request is rejected locally (`LsError::Invalid`) with no HTTP call. Type and required-ness always enforce; a value-class bound (enum/range/format) is permissive until the differential probe confirms it, so a valid request is never falsely rejected.

**Request field rules:**

- `IsuNo` — type `string`; required; alphanumeric symbol (permissive until confirmed)
- `OrdQty` — type `integer`; required
- `OrdPrc` — type `number`; required
- `BnsTpCode` — type `string`; required; one of [1, 2] (permissive until confirmed)
- `OrdprcPtnCode` — type `string`; required
- `MgntrnCode` — type `string`; required
- `LoanDt` — type `string`; optional
- `OrdCndiTpCode` — type `string`; required
- `MbrNo` — type `string`; optional

**Reachable gateway errors** (explained once from the shared catalog; environment/entitlement codes are not reproduced per TR):

- `00000` — Success. The gateway processed the request and returned valid data.
- `904` — The market session required for this TR is closed. An environment condition, not a request defect: the same request succeeds inside the correct trading window. Retry in-session.
- `01900` — This service is not provided in the Paper (모의투자) environment (모의투자에서는 해당업무가 제공되지 않습니다). A per-service capability gate: no Paper account can clear it. Not a problem with your request. Retry against a Production credential set, or treat the TR as paper-incompatible.
- `01491` — This paper account cannot place orders (모의투자 주문이 불가한 계좌입니다). A per-account capability gate distinct from 01900: the request reached the gateway and was cleanly rejected, nothing was placed. Swap in an order-enabled paper account to clear it.
- `01407` — A business-level order rejection observed on CSPAT00601's `IsuNo`-omitted submit variant (§30 differential, 2026-07-13). The required symbol was omitted; the gateway answered on merits and placed nothing (a distinct reject, not a may-rest). The caller fixes it by supplying `IsuNo`.
- `IGW40011` — A request field the gateway expects as a JSON number was sent as a quoted string. Serialize numeric request fields as JSON numbers (`string_as_number` / `string_as_decimal`) rather than strings. This is a request-shape defect the caller can fix.

