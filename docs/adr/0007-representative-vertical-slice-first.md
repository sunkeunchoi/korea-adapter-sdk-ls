# Representative vertical slice first

The new repository should prove the maintained-SDK architecture before expanding TR coverage. The first implementation tier will include core auth/client/transport/error/rate behavior, metadata index and representative per-TR metadata, and a small set of representative TRs across standalone, market-session, paginated, account, and realtime classes, while order support begins with metadata and safety/reconciliation design unless explicitly needed for runtime support.
