# Rust-first permanent tooling

The old repository's Python meta-layer became a maintenance burden that competed with SDK work. Permanent tooling for metadata validation, tracker planning, and change-scoped verification should be Rust-first where practical, while Python remains acceptable for one-time bootstrap scripts that migrate old specs or documents into maintained project data.
