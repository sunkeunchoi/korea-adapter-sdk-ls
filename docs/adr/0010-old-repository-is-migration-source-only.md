# Old repository is migration source only

`korea-broker-sdk-ls` contains useful runtime code, specifications, dependency documents, and lessons, but the new repository exists to obsolete its generated-surface architecture. The old repository will be used only as a migration source for selected code, metadata bootstrap, and reference documentation, and the new repository will not depend on old scripts, generated files, or release gates as permanent architecture.
